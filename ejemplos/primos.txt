principal {
    def variables
        entero n;
        entero d;
        entero p;
        caracter c;
        booleano div;
    enddef variables

    cin >>> n, c;
    p = 2;

    mientras (p < n) {
        d = 2;
        div = cierto;
        mientras (d < p) {
            div = div && (p % d != 0);
            d = d + 1;
        }
        si (div)
        entonces cout <<< c, p;
        p = p + 1;
    } 
}
